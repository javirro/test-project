export type Price = {
  price: number
  variation24h: number
}

export type UserBalanceWithProjectInfo = {
  username: number
  tokenMintAddress: string
  balance: number
  tokenName: string
  tokenSymbol: string
  image: string
}